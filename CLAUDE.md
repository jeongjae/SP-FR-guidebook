# 작업 규칙

Jason·Julia 의 2026 유럽 43일 여행 가이드북. **문서가 아니라 현장 도구다.**
2026-08-29 ~ 2026-10-10 · 43일 42박.

## 절대 규칙

1. **`site/` 를 직접 편집하지 않는다.** 다음 빌드에 덮어써진다.
   본문은 `source/`, 정본 계층은 `build/model.py`, 페이지는 `build/render.py`,
   CSS·JS 는 `build/assets/`.
   `source/CURRENT/30_Places/` 와 `20_Regions/` 도 편집하지 않는다 — 빌드가
   챕터 원고에서 다시 뽑는 **파생물**이다. 고칠 곳은 챕터 원고다.
2. **런타임 서드파티 의존성을 추가하지 않는다.** 현재 0개고 완전 오프라인으로 동작해야 한다.
   (`markdown`·`openpyxl` 은 빌드 전용, Leaflet 은 로컬 번들.)
3. **미확정값을 확정처럼 표시하지 않는다.** 예약이 아직 잠겨 있지 않다.
   숙소 후보 주소를 확정으로 믿고 이동하는 것이 이 프로젝트 최악의 사고다.
4. **검증 안 된 사실을 쓰지 않는다.** 현장에서 틀린 정보는 없느니만 못하다.
5. **이름을 번호로 짓지 않는다.** 지명으로 짓는다.
6. **main 에 직접 푸시하지 않는다.** 브랜치를 파고 PR 을 올린다.
   main 푸시는 즉시 배포로 이어진다.

## 사실 검증 기준

| 종류 | 처리 |
|---|---|
| 역사·건축·인물 | 복수 출처 확인 후 기술 |
| 운영시간·요금 | 기술하지 않거나 `{{badge:pending\|재확인}}` 필수 |
| 개인 해석 | 주어 명시 |
| 미확인 | **기술하지 않는다.** 추측으로 채우지 않는다 |

## 정보구조

**최소 단위는 '장소'다.** 갈 수 있으면 장소, 아니면 섹션이다.
일정표 라인아이템은 원자가 아니다 — `(날짜, 시각, 장소, 메모)` 의 조합이고
독립적 존재가 없다. 일정표 전체도 아니다, 하루라는 뷰다.

정본은 `source/ASSETS/91_Place_Registry_v1.0.md`. 빌드가 지도 핀·본문 등급
헤딩과 대조하고 어긋나면 중단한다. 편집은 이 MD 를 직접 고친다 —
`build/gen_place_registry.py` 를 다시 돌리면 손편집이 날아간다.

장소의 **장문**은 명부가 아니라 챕터 원고에 있고, 빌드가 매번
`source/CURRENT/30_Places/<slug>.md` 로 뽑아 쓴다. 지역의 편집 섹션
(Editor's Verdict · 꼭 경험할 세 장면 · 생략해도 되는 것 · 한눈에 보기)도
같은 방식으로 `20_Regions/<slug>.md` 로 나온다. **둘 다 파생물이라 직접
고치면 다음 빌드에 날아간다.** 고칠 곳은 챕터 원고다.

### 하나의 것은 하나의 페이지에만 있는다

**축은 목록을 맡고, 본문을 갖지 않는다.** 일자·지역·주제는 같은 콘텐츠를
세 방향에서 가리키는 색인이지 사본이 아니다.

하루의 페이지는 `daily/day-NN.html` 하나다. 한때 `chapters/<지역>/day-NN.html`
에도 같은 날이 있었고, 한쪽은 원고를, 다른 쪽은 원고를 정규식으로 다시 긁은
사본을 렌더했다. **43일 중 30일의 시간표가 서로 달랐고, 20일은 원고에 있는
피로도를 "없다"고 표시했다.** 현장에서 어느 쪽을 믿을지 고르게 만드는 구조는
그 자체로 사고다. 옛 주소는 리다이렉트만 남는다.

새 페이지를 만들기 전에 묻는다 — 이미 있는 것을 다른 축에서 다시 보여주는
것인가? 그렇다면 만들 것은 페이지가 아니라 링크다.

### 전체 메뉴(햄버거)는 두지 않는다

홈이 여정을, 하단탭이 축을, 꼬리말이 도구를 맡는다. 같은 목록을 네 번째로
늘어놓으면 메뉴가 사이트의 사본이 되고 어느 쪽이 최신인지 알 수 없게 된다.
상단바는 뒤로가기(이력 있을 때) + 위치 경로 + 검색이다. 검색만 시트로 연다.

메뉴를 지우기 전에는 링크 그래프로 확인한다 — 그 메뉴로만 갈 수 있는 곳이
남아 있으면 길이 끊긴다.

### 내비게이션 레벨

스트립마다 정확히 한 레벨만 맡는다.

```
L0 전역   하단탭     오늘 · 전체 일정 · 가이드 · 지도 · 준비   여행 행동을 고른다
L1 위치   상단바     위치 경로(빵부스러기) + 검색            지금 어디인지 안다
L2 형제   탭 스트립  지금 묶음 안에서                        옆으로 움직인다
```

데스크톱(≥768px)에서는 하단탭이 위로 올라간다. 순서·라벨·아이콘은 그대로다 —
같은 mental model 이어야 한다. 마우스가 화면 아래 끝까지 내려가지 않을 뿐이다.

L2 는 문맥마다 다르다. Day 페이지는 그 지역의 날들, 지역 페이지는 6개 섹션,
전체 일정은 지역 점프다.

**축 세 개를 나열하던 좌표 바는 없앴다.** 일자·지역·주제를 한 줄에 늘어놓던
것인데, 하단탭이 이미 같은 축을 맡고 있어 두 번째 사본이었다. 주제 축
(`topics/`)은 통째로 접었다 — 장소는 지역이, 하루는 일정이 이미 가리킨다.
옛 주소는 전부 리다이렉트로 남는다.

## 모든 변경의 마지막 — 빌드

**작업을 끝내기 전에 반드시 돌린다.**

```
python3 build/site.py
```

출력 경로는 `SPFR_SITE_DIR` 로 바꿀 수 있다. 같은 워크트리에서 다른 빌드가
동시에 돌 때 서로의 산출물을 지우지 않게 하기 위한 것이다.

**HIG 자동검사는 2026-08-18 에 제거했다가 같은 날 다시 만들었다.** 지운 것은
마크업 문자열을 고정하던 검사였고, 새로 만든 것은 **계산된 값**을 보는 검사다.
그래서 디자인을 바꿔도 깨지지 않고, 야외에서 못 읽는 화면만 잡는다.

```
python3 build/ux_check.py         # 명암비 · 하단탭 5개 · 데일리 43일 · 뷰포트 설정
python3 build/viewport_check.py   # 360·390·430·768·1024·1440 실렌더 측정
python3 build/content_audit.py    # 승격된 장문이 전부 렌더되는가 (손실 0)
```

`viewport_check` 는 가로 오버플로 · 터치 타깃 44pt · 글자 11px 하한을 실제로
렌더해서 잰다. 헤드리스 크로미움이 필요하다.

**여전히 사람이 봐야 하는 것** — 사진이 그 장소가 맞는가, 문장이 현장에서
읽히는가, 색이 의미와 맞는가. 기계는 값만 본다.

### 국기색은 의미로만 쓴다

팔레트는 스페인·프랑스 국기에서 나온다. 다만 색을 예쁘라고 칠하지 않는다 —
**빨강·금색은 스페인, 파랑·흰·빨강은 프랑스**이고, 상단바 아래 국기 띠는
"지금 어느 나라인가"를 말한다. Day 7 에 국경을 넘고 그 뒤로 시장 요일·
공휴일·긴급번호·언어가 바뀐다. 이동일은 띠가 반씩 나뉜다.

국기 원색은 '면'에만 쓴다. 금색 `#F1BF00` 은 흰 바탕에 1.7:1, 프랑스 빨강
`#EF4135` 는 3.4:1 이라 어느 쪽도 글자색이 될 수 없다. 글자가 필요하면
`--sig-*-ink` 를 쓴다.

### 아이콘은 CSS 마스크다

선 데이터는 `build/icons.py` 가 원본이고 출력이 `style.css` 뒤에 붙는다.
스프라이트를 페이지마다 인라인하면 314쪽이 각각 무거워진다 — 마스크는 CSS
한 번이고 페이지 무게는 0 이다. 색은 `currentColor` 를 받는다, 아이콘이
팔레트를 새로 만들지 않는다.

유니코드 도형(◉ ▤ ◇ ▧)을 쓰지 않는다. 기기마다 글자꼴 커버리지가 달라
어떤 기기에선 두부(□)가 뜬다. 현장에서 그건 못 쓰는 화면이다.

### 사진과 참고 링크는 근거가 있을 때만 건다

**사진은 적극적으로 채운다 — 등급을 가리지 않는다** (2026-08-18 결정).
판정 기준은 "재사용·재배포가 금지되지 않았는가"다. CC0·PD·CC BY·CC BY-SA 처럼
출처 표시로 쓸 수 있는 것은 쓴다. 다만 이 사이트는 gh-pages 로 **공개 배포**되므로
'개인 사용만 허용' 라이선스는 실제로는 쓸 수 없다 — 그건 재배포 금지다.

기록은 그대로 유지한다. 카탈로그(`data/media-catalog.json` 계열)에 라이선스·출처·
저작자가 없는 이미지는 빌드가 거부하고, 항목이 없으면 사진 자리를 아예 만들지 않는다.
저작자 표시가 필요한 라이선스는 화면에 표시한다. 위키백과는 참고 링크로만 쓴다 — 문서 제목은
`91_Place_Registry_v1.0.md` 의 `위키` 열이 정본이고, **확인한 것만 채운다.**
비슷한 이름으로 찍으면 남의 사진이 이 장소 것처럼 붙는다 — 현장에서 엉뚱한
곳을 찾게 된다. 제목이 없으면 링크도 만들지 않는다.

### 명암비는 HIG 최소치가 아니라 이 프로젝트 기준을 쓴다

야외 주광에서 읽는 도구다. Apple 의 secondaryLabel 계열은 이 용도에 모자란다.
본문 7:1, 보조 4.5:1 이 하한이다.

## 빌드 가드

빌드가 스스로 멈추는 검사. 끄지 않는다.

`build/site.py` 가 부르는 것:

| 가드 | 무엇을 막는가 |
|---|---|
| `model.validate` | 43일 전수 · 날짜 연속성 · 고아 장소 · 동선이 없는 지점을 가리키는 것 |
| `render.check_vocabulary` | 데이터에 새 이동수단·분류·등급이 들어왔는데 렌더러가 몰라 화면에 영어 코드가 새는 것 |
| `render.check_place_prose` | 원고 절 제목이 바뀌어 장소 페이지가 조용히 빈 껍데기가 되는 것 |
| `fact_guard` | 확정된 예약번호·전화·금액이 독자 정본에서 유실되는 것 |
| `content_guard` | 챕터 h2 구성·순서, 명부 ↔ dossier 대조, walk 필수 필드, 공식 출처 |

별도로 도는 것: `guards/run_all.py`(G1~G6) · `scripts/validate_itinerary.py` ·
`scripts/validate_media.py` · `build/pwa_check.py`(실제 오프라인 탐색) ·
`build/test_validation.py`(부정 픽스처 14종).

**어휘 가드가 특히 중요하다.** 콘텐츠 편집은 이 개편과 나란히 진행된다.
편집자가 새 값을 쓰면 렌더러가 그것을 모른 채 화면에 흘린다 — 실제로
`unconfirmed`(이동수단 미정)가 확정된 구간과 똑같이 보인 적이 있다.
현장에서 화면이 조용히 틀리는 것보다 빌드가 시끄럽게 멈추는 편이 낫다.
